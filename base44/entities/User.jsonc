{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "plan": {
      "type": "string",
      "description": "Subscription plan granted by the payments webhook",
      "enum": [
        "free",
        "pro"
      ],
      "default": "free"
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}